/// <reference path="../../typings/index.d.ts" />

import * as Should from 'should';

describe( `### INTEGRATION TESTS ###`, () => {
	it( '', ( done: ( error?: any ) => void ) => {
		setTimeout( () => {
			done();
		}, 500 );
	});
});